# GitHub Pages 部署指南

本指南将帮助你将兔兔爱叫连连看游戏部署到 GitHub Pages。

## 📋 前置要求

1. 一个 GitHub 账号
2. 已安装 Git
3. 已安装 Node.js (版本 18 或更高)

## 🚀 部署步骤

### 1. 创建 GitHub 仓库

1. 登录 GitHub
2. 点击右上角的 "+" 号，选择 "New repository"
3. 仓库名建议使用：`bunny-match-3`（如果使用其他名称，需要修改 `vite.config.ts` 中的 `base` 路径）
4. 设置为 Public（GitHub Pages 免费版需要公开仓库）
5. **不要**勾选 "Initialize this repository with a README"
6. 点击 "Create repository"

### 2. 初始化 Git 并推送代码

在项目根目录执行以下命令：

```bash
# 初始化 Git 仓库（如果还没有）
git init

# 添加所有文件
git add .

# 提交代码
git commit -m "Initial commit: Bunny Match-3 game"

# 添加远程仓库（将 YOUR_USERNAME 替换为你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/bunny-match-3.git

# 推送到 GitHub（如果仓库名不是 bunny-match-3，需要修改）
git branch -M main
git push -u origin main
```

### 3. 启用 GitHub Pages

1. 进入你的 GitHub 仓库页面
2. 点击右上角的 "Settings"（设置）
3. 在左侧菜单中找到 "Pages"
4. 在 "Source" 部分：
   - 选择 "GitHub Actions" 作为源
5. 保存设置

### 4. 触发部署

有两种方式触发部署：

**方式一：自动部署**
- 当你推送代码到 `main` 分支时，GitHub Actions 会自动构建并部署

**方式二：手动触发**
- 进入仓库的 "Actions" 标签页
- 选择 "Deploy to GitHub Pages" 工作流
- 点击 "Run workflow" 手动触发

### 5. 等待部署完成

1. 在 "Actions" 标签页查看部署进度
2. 等待构建和部署完成（通常需要 1-2 分钟）
3. 部署成功后，在 "Settings" > "Pages" 中可以看到你的网站地址

### 6. 访问你的游戏

部署完成后，你的游戏将在以下地址可访问：
```
https://YOUR_USERNAME.github.io/bunny-match-3/
```

## 🔧 如果仓库名不是 bunny-match-3

如果你的仓库名不同，需要修改 `vite.config.ts` 文件：

```typescript
const base = '/YOUR_REPO_NAME/';  // 替换为你的仓库名
```

例如，如果仓库名是 `my-bunny-game`，则：
```typescript
const base = '/my-bunny-game/';
```

## 📝 更新游戏

每次更新代码后，只需：

```bash
git add .
git commit -m "Update game"
git push
```

GitHub Actions 会自动重新构建和部署。

## 🐛 常见问题

### 问题 1: 页面显示 404
- 检查 `vite.config.ts` 中的 `base` 路径是否正确
- 确保仓库名和 base 路径匹配

### 问题 2: 资源加载失败
- 确保所有静态资源都在 `public` 目录下
- 检查资源路径是否使用相对路径

### 问题 3: 构建失败
- 检查 GitHub Actions 日志查看错误信息
- 确保 `package.json` 中的依赖都正确安装

### 问题 4: 部署后页面空白
- 检查浏览器控制台是否有错误
- 确保所有文件都已正确提交到仓库

## 📚 相关文档

- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [GitHub Actions 文档](https://docs.github.com/en/actions)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)

## 🎉 完成！

部署成功后，你就可以通过 GitHub Pages 链接分享你的游戏了！

