# 缓存问题解决方案

如果更新后手机上看不到变化，请按照以下步骤操作：

## 🔧 快速解决方案

### 方法1：强制刷新（推荐）
1. **Chrome/Edge（Android）**：
   - 打开游戏页面
   - 点击地址栏右侧的"..."菜单
   - 选择"重新加载"或长按刷新按钮选择"清空缓存并硬性重新加载"

2. **Safari（iOS）**：
   - 打开游戏页面
   - 长按刷新按钮
   - 选择"重新载入页面（不使用缓存）"

3. **通用方法**：
   - 关闭浏览器标签页
   - 完全关闭浏览器应用
   - 重新打开浏览器并访问游戏

### 方法2：清除浏览器缓存
1. **Chrome（Android）**：
   - 设置 → 隐私和安全 → 清除浏览数据
   - 选择"缓存的图片和文件"
   - 点击"清除数据"

2. **Safari（iOS）**：
   - 设置 → Safari → 清除历史记录和网站数据

### 方法3：使用无痕/隐私模式
- 打开浏览器的无痕/隐私模式
- 访问游戏页面
- 这样可以绕过缓存，看到最新版本

### 方法4：检查版本号
1. 打开游戏页面
2. 打开浏览器开发者工具（F12 或长按刷新按钮选择"检查"）
3. 在控制台（Console）中查看版本号
4. 如果版本号是旧的，说明缓存未清除

## 🚀 开发者操作

### 重新构建和部署
```bash
# 1. 确保代码已提交
git add .
git commit -m "Update: 修复缓存问题"
git push

# 2. 重新构建（如果需要）
npm run build

# 3. 确保 dist/ 目录已更新
# 4. 推送到 GitHub Pages
```

### 验证更新
1. 检查 `dist/index.html` 中是否有最新的代码
2. 检查文件是否有新的 hash 值（Vite 会自动添加）
3. 在浏览器中访问：`https://你的用户名.github.io/仓库名/`
4. 打开开发者工具，查看 Network 标签，确认文件是从服务器加载而不是缓存

## 📱 移动端特殊处理

### Android Chrome
1. 打开 `chrome://settings/clearBrowserData`
2. 选择"缓存的图片和文件"
3. 时间范围选择"全部时间"
4. 清除数据

### iOS Safari
1. 设置 → Safari → 高级 → 网站数据
2. 找到你的游戏网站
3. 删除网站数据

## 🔍 调试技巧

### 检查是否更新成功
在浏览器控制台运行：
```javascript
// 查看版本号
console.log(document.documentElement.getAttribute('data-version'));

// 检查 Service Worker
navigator.serviceWorker.getRegistrations().then(regs => {
  console.log('Service Workers:', regs.length);
});

// 检查缓存
caches.keys().then(keys => {
  console.log('Cache Keys:', keys);
});
```

### 强制清除所有缓存（控制台运行）
```javascript
// 清除所有 Service Worker
navigator.serviceWorker.getRegistrations().then(function(registrations) {
  for(let registration of registrations) {
    registration.unregister();
  }
});

// 清除所有缓存
caches.keys().then(function(names) {
  for (let name of names) {
    caches.delete(name);
  }
});

// 重新加载
location.reload(true);
```

## ⚠️ 常见问题

### Q: 为什么更新后还是旧版本？
A: 可能是以下原因：
1. GitHub Pages 的 CDN 缓存（通常几分钟到几小时）
2. 浏览器缓存未清除
3. Service Worker 缓存
4. 文件未正确上传

### Q: GitHub Pages 更新需要多久？
A: 通常 1-5 分钟，但有时可能需要更长时间。可以：
1. 等待 5-10 分钟后再试
2. 在 URL 后添加 `?v=时间戳` 强制刷新
3. 检查 GitHub Actions 是否构建成功

### Q: 如何确认文件已更新？
A: 
1. 检查 GitHub 仓库中的文件时间戳
2. 检查 dist/ 目录中的文件
3. 在浏览器 Network 标签中查看文件的响应头（应该没有 `from cache`）

## 💡 预防措施

代码中已添加：
- ✅ 缓存控制 meta 标签
- ✅ Service Worker 自动清除
- ✅ 版本号记录
- ✅ Vite 构建时自动添加文件 hash

这些措施可以最大程度减少缓存问题。

